{
  "version": 1,
  "hooks": {
    "stop": [
      {
        "type": "prompt",
        "prompt": "Before finishing, check if any source files were modified in this session (look at your tool call history for Write/StrReplace calls to .ts/.tsx files under src/).\n\nIf source files WERE modified:\n1. Run `pnpm test` to execute the test suite.\n2. If all tests pass, report the result briefly.\n3. If any tests fail, analyze the failure:\n   - If the failure is caused by your changes, fix the code and re-run tests.\n   - If the test expectation is outdated due to an intentional change, update the test.\n4. If you modified or added business logic in a file that has a corresponding test file in src/__tests__/, check if the existing tests still cover the new behavior. If not, add a focused test for the new logic.\n5. If you created a new utility function or reducer action with non-trivial logic (branching, loops, math), add at least one test for the happy path and one edge case.\n\nIf locale files (en.json, ru.json) were modified:\n6. Read both `src/assets/i18n/locales/en.json` and `src/assets/i18n/locales/ru.json`.\n7. Compare their top-level and nested keys. Every key in en.json must exist in ru.json and vice versa.\n8. If keys are missing in either file, add them (use the English value as a placeholder stub in ru.json if no translation is available, prefixed with '[RU] ').\n9. Report any keys that were added or fixed.\n\nIf NO source files were modified (only docs, config, or no changes at all), skip testing and i18n checks.\n\nHook input: $ARGUMENTS",
        "timeout": 120,
        "loop_limit": 3
      }
    ]
  }
}
